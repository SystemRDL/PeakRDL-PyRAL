Hierarchical HWIO
=================

.. todo::
    In some embedded applications, it may not be practical to use a single HWIO
    connection for a RAL.
    Mapping internal RAL nodes --> specific device files (such as uio)

    Show SoC example from before but attach different HWIOs to each peripheral. Use UIO device paths as examples
