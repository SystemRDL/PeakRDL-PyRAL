Grafting together multiple RALs
===============================

.. todo::
    - Describe vertical re-use. why you'd want this
    - Show a simple set of example RDL files (2 subcomponents, instantiated twice each + a "soc top")
    - Example 1: monolithic RAL
        Show a single export command
            Be sure to also show mkdir of a myrals output dir, and touch of __init__.py. Mention why!
        Show a tree diagram of the RAL. Box around it labeled the py file
    - Example 2: Distributed RAL
        Show three export commands
        Same tree diagram, but show three separate files

    - Show python code importing from SoC-level RAL, and referencing stuff in either subsystems
